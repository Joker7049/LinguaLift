CREATE TABLE ai_memory(
    id INTEGER NOT NULL PRIMARY KEY,
    summary TEXT NOT NULL
);

getMemory:
SELECT summary FROM ai_memory WHERE id = 1;

updateMemory:
INSERT OR REPLACE INTO ai_memory (id, summary)
VALUES (1, ?);